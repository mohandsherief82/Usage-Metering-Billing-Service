"""Seed a couple of demo tenants + plans for local dev / TUI screenshots."""

# TODO: create_engine, insert demo Plan("free", quota=1000), Plan("pro", quota=100000),
#       Tenant("acme", plan="pro"), Tenant("beta", plan="free")

if __name__ == "__main__":
    raise NotImplementedError("wire up once db/models.py is implemented")
